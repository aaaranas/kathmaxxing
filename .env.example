# Neon Postgres connection string.
#
# Create a project at https://neon.tech, then copy the *pooled* connection
# string from the dashboard (the one whose host contains "-pooler").
#
# Copy this file to .env.local and fill in the value. Until it is set, the
# converter works normally and the History tab shows setup instructions
# instead of erroring.
DATABASE_URL="postgresql://neondb_owner:npg_eSbfTKkj9o1C@ep-flat-band-ae0w9etc-pooler.c-2.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require"
